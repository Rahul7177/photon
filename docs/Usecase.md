Understand my usecase carefully.
Usecase : I want to build a vs code extension that helps users to use local llm models (low end for ex: qwen 2.5 coder, gemma 4 E4B, qwen 3 8B, etc.) through ollama.
The current issue is there are extensions like continue, copilot chat that has same features but they aer very bad at handling the low end models on a low end machine. The models are not able to handle their complex tools, instructions (that works best for high end models like claude). Hence either the model's context window (low around 8-16k gets filled just due to these tools, instructions, etc.) or the model is not able to make the intelligent decisions.
To solve this gap, I am trying to build "Photon" an extension that looks very similar like copilot chat, or claude chat that connects to ollama , allows user to choose any of the available locally hosted models, chosse modes : Agent, Plan, Chat, great adaptive mcp's, tools to read, edit files in workspace, run terminal commands, search internet, etc. Make sure the tools are designed in a way that these low parameter models are able to understand and use it. 
The way photon should handle the models is it will read the specs of the machine, the model and suggests the best settings and the instructions passed to the model along with the user prompt in such a way that the model is actually able to handle it.
The extension chat window should open like a sidebar, and looks very similar to claude and copilot chat extensions for vs code. The Ux is also very improtant, so that every thing like models, the context window, etc should be controllable by the user. Use color like red, orangish yellow and dark space black, to match the name "Photon" with proper typography.
